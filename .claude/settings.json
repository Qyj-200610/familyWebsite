{
  "permissions": {
    "allow": [
      "Bash(mysql -u root -p\"$MYSQL_ROOT_PASSWORD\" -e \"USE family_website; SHOW TABLES; SELECT id, username, email FROM users;\")",
      "Bash(mysql -u root -p\"$MYSQL_ROOT_PASSWORD\" -e \"USE family_website; DESCRIBE users; SELECT * FROM users WHERE id=1;\")"
    ]
  }
}
